"use client";

// Thin default export so the dashboard layout and existing import sites keep
// resolving. The full workspace implementation lives in the modular
// `ai-workspace/` folder.

export { default } from "@/components/ai-workspace/AIWorkspace";